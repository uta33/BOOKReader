# Public Worker URL used only to issue the browser's anonymous bearer token.
VITE_WORKER_API_BASE_URL=http://localhost:8787

# Vite proxies /api to this local Worker port.
API_PORT=8787

# Vercel Functions use this server-side URL in preview/production.
WORKER_API_BASE_URL=http://localhost:8787
