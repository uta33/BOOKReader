{
  "name": "bookreader-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "test:unit": "tsx tests/limits.test.mts && tsx tests/quota.test.mts && tsx tests/diagnostics.test.mts && tsx tests/book-covers.test.mts && tsx tests/attachments.test.mts && tsx tests/sync.test.mts && tsx tests/merge.test.mts && tsx tests/oauth.test.mts",
    "test:smoke": "node tests/smoke-local.mjs",
    "test": "npm run typecheck && npm run test:unit",
    "db:migrate:local": "wrangler d1 migrations apply bookreader --local",
    "db:migrate:remote": "wrangler d1 migrations apply bookreader --remote",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "hono": "^4.12.32",
    "jose": "^6.2.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260728.1",
    "tsx": "^4.20.6",
    "typescript": "~6.0.3",
    "wrangler": "^4.114.0"
  }
}
